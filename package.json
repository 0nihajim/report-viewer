{
  "name": "report-viewer",
  "version": "0.1.0",
  "private": true,
  "description": "Private, mobile-first HTML report viewer on Cloudflare Workers + R2, gated by Cloudflare Access",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260730.1",
    "typescript": "^5.7.2",
    "wrangler": "^4.118.0"
  }
}
